# Changelog

All notable crate-specific changes for `rs_kv_validation_harness` are recorded here.

## [Unreleased]

### Added

- Wave 31: added `positional_index_entry_round_trip` and `positional_index_error_shape` validation cases using `PositionalIndexEntry`, `PositionalIndexKey`, `ContentHash`, `SequenceHash`, and `PositionalIndexError` from `rs_kv_quant_contracts`.
- Added crate-local documentation coverage with `README.md` and `CHANGELOG.md` tracking for `rs_kv_validation_harness` (0.1.0).
