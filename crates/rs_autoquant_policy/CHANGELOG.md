# Changelog

All notable crate-specific changes for `rs_autoquant_policy` are recorded here.

## [Unreleased]

### Added

- Added crate-local documentation coverage with `README.md` and `CHANGELOG.md` tracking for `rs_autoquant_policy` (0.1.0).
- Added observer-snapshot round-trip coverage for the autoquant policy crate.
- Added Rust-native AutoQuant backend summaries, codec-choice parsing, and backend-chain dispatch modeling.
- Added `tq1` and `tq8` codec-name support so the policy crate matches the shared codec contract.
