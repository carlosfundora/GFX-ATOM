[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "rs-codec"
version = "0.2.0"
description = "High-performance audio codec and DSP kernels with PyO3 bindings"
requires-python = ">=3.11"

[tool.maturin]
manifest-path = "rs_codec/Cargo.toml"
